import { makeStyles } from "@material-ui/core";


export default makeStyles((theme) => ({
    advertDetailsContainer:{
        marginTop: theme.spacing(10)
    },
    advertDetailsElements: {
        listStyle: 'none',
        margin: 2
    }
}))